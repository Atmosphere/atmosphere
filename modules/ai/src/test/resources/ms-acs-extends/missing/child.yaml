agent_control_specification_version: "0.3.1-beta"
extends:
  - does-not-exist.yaml
policies:
  test_policy:
    type: test
intervention_points:
  input:
    policy_target: "$snap.input"
    policy:
      id: test_policy
