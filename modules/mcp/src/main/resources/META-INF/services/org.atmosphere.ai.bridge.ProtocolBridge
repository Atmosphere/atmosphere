# McpProtocolBridge is registered programmatically by McpCoordinatorRegistration / McpAgentRegistration
# via ProtocolBridgeRegistry.install(...) — it requires an AtmosphereFramework
# reference at construction, so a no-arg ServiceLoader entry would raise a
# ServiceConfigurationError. This file stays present as the contract-visible
# registration point for third-party bridges with no-arg constructors.
