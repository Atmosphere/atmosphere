name: E2E Tests

on:
  push:
    branches: [ '**' ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
  workflow_dispatch:

jobs:
  build:
    name: Build (SB${{ matrix.spring-boot-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        spring-boot-version: [4, 3]
      fail-fast: false
    steps:
    - uses: actions/checkout@v6

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: 21
        distribution: 'temurin'
        cache: 'maven'

    - name: Build All Modules
      run: ./mvnw -B clean install -DskipTests -Dgpg.skip=true

    - name: Rebuild Spring Boot samples with SB3
      if: matrix.spring-boot-version == 3
      run: |
        SB_SAMPLES=$(ls -d samples/spring-boot-*/pom.xml | grep -v embabel | sed 's|/pom.xml||' | paste -sd, -)
        ./mvnw -B package -DskipTests -Dgpg.skip=true -Pspring-boot3 -pl "$SB_SAMPLES"

    - name: Package build output
      run: |
        tar czf /tmp/build-targets-sb${{ matrix.spring-boot-version }}.tar.gz \
          samples/*/target \
          modules/*/target \
          -C "$HOME" .m2/repository/org/atmosphere

    - name: Upload build output
      uses: actions/upload-artifact@v5
      with:
        name: build-targets-sb${{ matrix.spring-boot-version }}
        path: /tmp/build-targets-sb${{ matrix.spring-boot-version }}.tar.gz
        retention-days: 1

  e2e:
    name: E2E — ${{ matrix.group.name }} (SB${{ matrix.spring-boot-version }})
    runs-on: ubuntu-latest
    needs: [build]

    strategy:
      matrix:
        spring-boot-version: [4, 3]
        group:
          - name: core-transports
            projects: "chat,spring-boot-chat,multi-client,embedded-jetty-chat,quarkus-chat,sse-transport,long-polling-transport,transport-fallback,reconnection"
          - name: ai-chat
            projects: "ai-chat,ai-streaming-dom,ai-chat-features,ai-memory,ai-memory-strategies,ai-identity,ai-error-recovery,ai-events,unified-console"
          - name: ai-tools-adapters
            projects: "ai-tools,ai-session-stats"
          - name: ai-advanced
            projects: "rag-chat,ai-filters,ai-fanout,ai-cache,ai-budget,ai-cost-routing,ai-cache-coalescing,ai-combined-cost-cache,ai-routing,ai-classroom,spring-boot-ai-classroom"
          - name: protocols
            projects: "mcp-server,mcp-tools,a2a-agent,agui-chat,multi-agent-startup-team,grpc-browser,rooms-api,room-typing-direct"
          - name: resilience
            projects: "durable-sessions,durable-session-token,durable-session-identity,otel-chat,chat-observability,offline-queue,history-cache,xss-protection,auth-rejection,auth-token"
          - name: stress
            projects: "message-ordering,large-payload,slow-consumer,connection-timeout"
          - name: gap-analysis
            projects: "redis-clustering,kafka-clustering,kotlin-dsl,wasync-client,mcp-bidirectional,cross-transport-interop,agui-sse-lifecycle,channel-gateway,otel-span-correlation,a2a-multi-hop,concurrent-protocol-access,coordinator-remote,coordinator-journal,durable-session-restart,session-token-expiry,auth-oauth-jwt,spring-boot3-parity,backpressure-bounded-queue,webtransport-raw"
      fail-fast: false

    steps:
    - uses: actions/checkout@v6

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: 21
        distribution: 'temurin'
        cache: 'maven'

    - name: Download build output
      uses: actions/download-artifact@v8
      with:
        name: build-targets-sb${{ matrix.spring-boot-version }}

    - name: Restore build output
      run: |
        tar xzmf build-targets-sb${{ matrix.spring-boot-version }}.tar.gz
        # Restore atmosphere SNAPSHOT artifacts to local Maven repo
        if [ -d .m2/repository/org/atmosphere ]; then
          mkdir -p ~/.m2/repository/org
          cp -r .m2/repository/org/atmosphere ~/.m2/repository/org/
          rm -rf .m2
        fi

    - name: Set up Node.js
      uses: actions/setup-node@v6
      with:
        node-version: 22
        cache: 'npm'
        cache-dependency-path: modules/integration-tests/package-lock.json

    - name: Install Playwright Dependencies
      working-directory: modules/integration-tests
      run: |
        npm ci
        npx playwright install --with-deps chromium

    - name: Run E2E Tests
      working-directory: modules/integration-tests
      env:
        LLM_MODE: fake
        SMOKE_ONLY: ${{ github.event_name == 'pull_request' && 'true' || '' }}
        INCLUDE_FLAKY: 'false'
      run: |
        PROJECTS=$(echo "${{ matrix.group.projects }}" | sed 's/,/ --project=/g')
        npx playwright test --project=$PROJECTS --reporter=list,github

    - name: Upload Playwright Report
      if: failure()
      uses: actions/upload-artifact@v5
      with:
        name: playwright-report-${{ matrix.group.name }}-sb${{ matrix.spring-boot-version }}
        path: modules/integration-tests/playwright-report/
        retention-days: 14
