# Disable GitHub's automatic Maven dependency submission.
# The default 'mvn validate' fails because inter-module SNAPSHOT
# dependencies (e.g. atmosphere-ai → atmosphere-runtime) are not
# resolvable without a full reactor build.
# A custom workflow (.github/workflows/dependency-submission.yml)
# handles this correctly by building first.
maven:
  disabled: true
