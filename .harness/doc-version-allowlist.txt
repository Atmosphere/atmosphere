# Allowlist for scripts/validate-doc-version-alignment.sh
#
# One substring per line. If a flagged "<file>:<line>: <Label> <version>"
# record contains any of these substrings, it is exempted. Use for
# intentional mentions of a non-pinned version: transitive dependencies,
# historical comparisons, or upgrade-path prose.
#
# Lines starting with '#' are comments. Keep each entry justified.
#
# (No active exceptions today. Spring AI / Spring AI Alibaba are not tracked
#  by the gate at all — the tree legitimately carries two Spring AI lines,
#  2.0.0-M6 on the main classpath and 1.1.x via the Alibaba adapter — so no
#  allowlist entry is needed for them.)

# Historical fact in the 4.0.51 MCP oversell post-mortem timeline: atmosphere.js
# 5.0.29 is the version that was actually published during that release. The
# pinned dev version is now 5.0.30; rewriting the post-mortem to 5.0.30 would
# falsify the record (5.0.30 was never published). Intentional historical mention.
atmosphere.js 5.0.29
