server:
  port: 8084

atmosphere:
  packages: org.atmosphere.samples.springboot.a2a

llm:
  mode: ${LLM_MODE:remote}
  model: ${LLM_MODEL:gemini-2.5-flash}
  base-url: ${LLM_BASE_URL:}
  api-key: ${LLM_API_KEY:${GEMINI_API_KEY:}}

logging:
  level:
    org.atmosphere: DEBUG
