server:
  port: 8081

atmosphere:
  ai:
    runtime: spring-ai

spring:
  application:
    name: atmosphere-coding-agent
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o-mini}
